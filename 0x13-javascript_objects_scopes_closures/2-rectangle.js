#!/usr/bin/node

//class Rectangle that defines a rectangle

class Rectangle {
    constructor(width, height){
        if ((w > 0) && (h > 0)) {
        this.width = 'w';
        this.height = 'h';
        }
    }
}

module.exports = Rectangle;